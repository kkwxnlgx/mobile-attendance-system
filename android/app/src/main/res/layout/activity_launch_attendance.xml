<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <include layout="@layout/view_toolbar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <androidx.cardview.widget.CardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardBackgroundColor="@color/card"
                app:cardCornerRadius="14dp"
                app:cardElevation="2dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="18dp">

                    <TextView
                        android:id="@+id/tv_course"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="课程"
                        android:textColor="@color/text_primary"
                        android:textSize="18sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/tv_class"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="6dp"
                        android:text="班级 · 地点"
                        android:textColor="@color/text_secondary"
                        android:textSize="13sp" />
                </LinearLayout>
            </androidx.cardview.widget.CardView>

            <TextView style="@style/FormLabel" android:text="签到有效时长（分钟）" />
            <EditText android:id="@+id/et_duration" style="@style/FormInput"
                android:inputType="number" android:text="30" />

            <TextView style="@style/FormLabel" android:text="迟到判定阈值（开始后 N 分钟）" />
            <EditText android:id="@+id/et_late" style="@style/FormInput"
                android:inputType="number" android:text="10" />

            <TextView style="@style/FormLabel" android:text="允许打卡半径（米）" />
            <EditText android:id="@+id/et_radius" style="@style/FormInput"
                android:inputType="number" android:text="300" />

            <Button
                android:id="@+id/btn_launch"
                style="@style/PrimaryButton"
                android:layout_marginTop="30dp"
                android:text="发 起 考 勤" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:gravity="center"
                android:text="发起后系统生成签到码，并按班级名单建立考勤记录"
                android:textColor="#AEB6BD"
                android:textSize="11sp" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
