# 配置模板。复制成 .env 再填真实值：
#   Windows: copy .env.example .env
# .env 含密码不提交；本文件不含密码，可提交进仓库给协作者参考。

DB_PASSWORD=

# 以下都有默认值，按需覆盖：
# DB_BACKEND=mysql        # 改成 sqlite 可绕开 MySQL 用本地文件库
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=root
# DB_NAME=attendance_db
